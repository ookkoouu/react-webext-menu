{
  "name": "@okou/react-webext-menu",
  "version": "0.0.0",
  "description": "React for browser extension contextmenu",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "npm run format && npm run test",
    "format": "prettier -w src test && biome check --write src test",
    "test": "vitest run"
  },
  "files": [
    "src"
  ],
  "dependencies": {
    "fast-deep-equal": "^3.1.3",
    "react-reconciler": "^0.29"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/react-reconciler": "^0.28",
    "@types/webextension-polyfill": "*",
    "lefthook": "^1.10.0",
    "prettier": "^3.4.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  },
  "peerDependencies": {
    "@types/webextension-polyfill": "^0.12.0"
  }
}
